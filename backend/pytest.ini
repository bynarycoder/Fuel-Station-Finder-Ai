[pytest]
# Auto-collect async tests so handlers/dependencies can be exercised with
# pytest-asyncio without per-test markers.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Live Groq integration smoke tests run only when GROQ_API_KEY is set.
markers =
    integration: live integration tests requiring external API keys

